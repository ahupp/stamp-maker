{
  "name": "stamp-maker-www",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "wasmbuild": "cd ../wasm; wasm-pack build --target web --out-name index --release",
    "wwwbuild": "vite build",
    "deploy": "yarn run wasmbuild && yarn run wwwbuild",
    "start": "yarn run deploy && vite preview"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "vite": "4.1.1"
  },
  "dependencies": {
    "stamp-maker-wasm": "../wasm/pkg",
    "three": "^0.148"
  },
  "packageManager": "yarn@3.3.1"
}
